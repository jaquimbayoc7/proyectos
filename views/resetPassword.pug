<!DOCTYPE html>
html(lang="en")
    head
        include partials/header
    body.login
        .contenedor-formulario
            h1= nombrePagina
            p Agrega tu Nuevo Password para Acceder a tu Cuenta
            form.caja-login(method="POST")
                .campo
                    label(for="password") Nuevo Password:
                    input(type="password" name="password" placeholder="Tu Password..")
                .campo.enviar
                    input.boton(type="submit" value="Restablecer")          